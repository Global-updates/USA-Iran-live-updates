rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read access for the landing page content.
    // Restrict writes to signed-in admins or your preferred auth logic.
    match /siteContent/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
